services:
  db:
    image: postgres:15
    container_name: linha-tempo-postgres
    restart: always
    env_file:
      - .env
    environment:
      TZ: America/Sao_Paulo
      POSTGRES_DB: ${DB_NAME:-linha_tempo_db}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "${DB_PORT:-5432}:5432"
    networks:
      - linha-tempo-net
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-linha_tempo_db}"]
      interval: 5s
      timeout: 10s
      retries: 15
      start_period: 20s

  app:
    build: .
    container_name: linha-tempo-api
    env_file:
      - .env
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/${DB_NAME:-linha_tempo_db}
      SPRING_DATASOURCE_USERNAME: ${DB_USER:-postgres}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD:-postgres}
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.PostgreSQLDialect
      SERVER_PORT: 8080
    ports:
      - "${SPRING_SERVER_PORT:-8080}:8080"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - linha-tempo-net

networks:
  linha-tempo-net:
    driver: bridge

volumes:
  postgres-data: